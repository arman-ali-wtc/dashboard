import { Inter, Lusitana } from 'next/font/google';

export const inter: ReturnType<typeof Inter> = Inter({ subsets: ['latin'] });
export const lusitana: ReturnType<typeof Lusitana> = Lusitana({ weight: ['400', '700'], subsets: ['latin'] });
